import styled from "styled-components";
import { colors } from "../../../Utils/colors";
import { FavoriteButtonStyleProps } from "./FavoriteButton";

export const Button = styled.div`
  cursor: pointer;
`;
